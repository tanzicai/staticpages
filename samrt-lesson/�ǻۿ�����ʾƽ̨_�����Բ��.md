# 智慧课堂演示平台 ·《功能自查表》

> ## V3.2 更新记录（2026-09-09 —— 视频源收敛：仅 B 站在线课堂视频）
> 1. 播放界面只保留 **B 站教师课堂实录（BV1Hi4y1u7Zi，player.bilibili.com 官方外嵌，联网即播）**；**本地视频通道已彻底移除**（js 不再加载 assets 下 mp4、无“本地视频”入口、`assets/课堂视频放置说明.txt` 已删除）。
> 2. 播放器内仅剩两个画面源：「B站课堂视频（默认）/ 课件画面（断网兜底）」；字幕为播放器下方“AI 实时字幕条”（开/关 + 中文/English），示例字幕按时间轴演示“识别→字幕→翻译”链路（接入云端 ASR/MT 后按真实语音时间戳生成）。
> 3. 首页“平台对接状态”课堂视频源显示为“B站教师课堂实录 · 在线 · 已接入”。
> 4. 验证：专项冒烟（iframe 挂载/bvid/字幕开关/课件画面切换/督导页播放）+ 全量回归 ALL_PASS、0 报错。

> ## V3 更新记录（2026-09-09 第三次修订 —— 布局改版 + 真实课堂视频）
> 1. **整体布局改版**：导航栏由左侧改至**顶部**（工作台/课程中心/课堂分析/资源中心/督导评课/系统管理 6 大分组 + 下拉子菜单），页面内容全宽展示。
> 2. **首页大模块直达**：工作台改为“问候条 + 数据概览 + 10 张大模块卡片（点击即跳转）+ 对接状态/最新动态”，一屏进入各功能。
> 3. **真实课堂视频统一嵌入**：所有视频播放界面（直录播资源播放、在线督导课堂视频、课堂活动关联视频引用、语音转写页“查看课堂实录”）统一播放 B 站在线课堂视频（见顶部 V3.2 记录，本地 mp4 方式已移除）。*（历史记录：V3 时曾为加载 assets/课堂视频.mp4 的真实 HTML5 视频，V3.2 起已不再使用本地文件。）*
> 4. **字幕交互层**：播放器内置“字幕：开/关 + 中/英”字幕条（B站/课件模式下示例字幕按时间轴滚动，本地 mp4 模式已移除）；标注“接入云端语音识别后自动生成真实字幕”。
> 5. 冒烟：新版式回归 16 路由全部渲染 + 模块跳转/活动增删/上传/督导打分/语音转写全流程 ALL_PASS、0 报错。


> ## 本轮更新记录（2026-09-09 二次修订，以下内容以最新版为准）
> 1. 课程扩充至 5 门：新增「外科学（普外总论）」「儿科学（呼吸系统常见病）」，含授课计划 / 大纲知识点 / 课堂活动 / 排课节次，直录播资源增至 9 条。
> 2. 督导评课切换课程/节次后，紫色信息栏 8 字段联动刷新，周进度随所选节次所在周动态计算（自动化验证 c4/c5 切换 OK）。
> 3. 评价表新增「参数设置」：督导评课界面与评价表管理均可在线编辑维度/指标/分值/说明（自动化验证新增维度后 5→6 生效）。
> 4. 直录播上传与云端资源库上传启用本机文件选择窗口（读取真实文件名/大小）。
> 5. 权限开放范围=指定班级时弹出班级多选、云端资源库=指定教师时弹出教师多选（保存后生效并展示）。
> 6. 角色化菜单：系统管理员（平台管理员）视角下导航与标题为「课程库」，普通教师为「我的课程」。
> 7. 语音转写支持选择不同课堂实录资源（7 段样例语音 + 各自多语种译文），切换后按该资源转写并保存。
> 8. 直录播资源新增「平台调用」闭环：绑定课堂活动 / 加入课程资料区 / 作为知识点覆盖证据，可查引用并可取消，对应场景可直接播放引用。
> 9. 验证：两轮 jsdom 冒烟（16 路由 + 新功能增量）全部通过、0 报错。

> 项目：智慧课堂（AI课堂分析平台）演示平台
> 目录：`智慧课堂演示平台/`　运行方式：直接双击打开 `index.html`（登录后进入系统）
> 测试方式：登录页 + jsdom 自动化冒烟测试（16 个路由全部渲染、关键业务全流程跑通、控制台 0 报错）+ 人工走查核对
> 账号：`root`　密码：`root`（登录页不显示任何账号密码提示，观感与正式系统一致）


---

## 演示参数 1：课堂活动同步 · 时间轴

- 来源：演示参数
- 实现等级：真实实现
- 所属页面：课堂分析 →「课堂活动」（`#/activity`）、课程中心 →「对接课程配置」（`#/course-auth`）
- 实现方式：活动数据存 localStorage；`立即同步`从“教学平台待同步池”真实拉取并插入时间轴；时间轴按日期分组，支持课程/类型/来源/时间范围筛选；活动支持发布、编辑、移除；「教学平台空间」以页面内嵌浏览器形态展示平台课程空间参与情况；「同步配置」可维护每门课程的同步范围与策略（存入 `db.courseSync` 并持久化）。

### 实现明细（逐条）

| # | 交互点 / 需求 | 是否实现 | 如何验证 |
|---|---|---|---|
| 1 | 页面顶部统计本月活动/同步/发布次数/参与率 | ✅ | 进入「课堂活动」可见 4 张统计卡，数字随活动增删实时变化 |
| 2 | 校方教学平台接入状态与最近同步时间展示 | ✅ | 活动列表上方有「对接正常」绿色标签 + 最近同步时间 |
| 3 | 点「立即同步」真实拉取教学平台活动 | ✅ | jsdom 实测：同步池 3 项 → 同步后为 0，活动列表新增 3 条“教学平台同步”活动并置顶 |
| 4 | 时间轴以时间线呈现随堂练习等活动 | ✅ | 时间轴按日期分组，每条含类型徽标、时间、周次、来源、参与数 |
| 5 | 活动按课程 / 类型 / 来源 / 时间范围筛选 | ✅ | 课程下拉、类型 chip 多选、全部来源/同步/本平台、全部/本月/本周均实时生效 |
| 6 | 发起活动（随堂练习/主题讨论/分组任务等）真实入列表 | ✅ | jsdom 实测：活动数 28→29，新活动以“进行中”状态出现在时间轴顶部 |
| 7 | 编辑自己发布的活动真实生效 | ✅ | 「编辑」表单回填，保存后标题/类型更新并写操作日志 |
| 8 | 移除活动（含确认）真实删除 | ✅ | jsdom 实测：29→28 恢复原数；删除同步活动时提示“仅影响本平台视图” |
| 9 | 活动参与详情弹窗（人数/得分/名单） | ✅ | 点「参与详情」出现统计卡与参与名单表 |
| 10 | 跳转教学平台课程空间查看参与情况 | ✅ | 点「教学平台空间」打开模拟浏览器：地址栏显示教学平台课程空间 URL、活动实时数据与参与名单，含“统一身份认证已通过”标识 |
| 11 | 同步配置（课堂活动记录范围/策略）保存生效 | ✅ | 「同步配置」勾选课程/类型/策略保存后写入 `courseSync`，重启页面仍保留 |
| 12 | 操作留痕 | ✅ | 发布/编辑/移除/同步均写入「操作日志」与消息通知 |

### 自验结果
- 是否跑通：✅ jsdom 自动化 + 人工逐项核对均通过

---

## 演示参数 2：授课计划比对 · 大纲知识点覆盖 · 分析报告

- 来源：演示参数
- 实现等级：真实实现
- 所属页面：课堂分析 →「授课计划比对」（`#/teachplan`）、「大纲覆盖分析」（`#/outline`）、「分析报告」（`#/reports`）
- 实现方式：以“内科学（呼吸系统疾病）”等 3 门课程为数据样本，授课计划条目由大纲知识点自动生成（含周次/学时/匹配置信度），覆盖状态由课堂活动与授课记录自动累计；「重新自动匹配」「重新计算」带加载动画真实重算并刷新覆盖率；「分析报告」汇总两类覆盖统计并可下载 TXT / 打印。

### 实现明细（逐条）

| # | 交互点 / 需求 | 是否实现 | 如何验证 |
|---|---|---|---|
| 1 | 展示教学平台授课计划匹配来源与更新时间 | ✅ | 页面显示平台课程号（JXP-C0231 等）与最近匹配时间 |
| 2 | 授课计划覆盖率自动计算（授课知识点/匹配覆盖对比） | ✅ | 覆盖率卡 = 覆盖学时/计划学时；明细表逐条列出知识点、计划周次/学时、课堂覆盖、匹配结果 |
| 3 | 每条知识点匹配结果与覆盖证据 | ✅ | 「覆盖证据」弹窗展示关联课堂活动标题、参与数与平均分 |
| 4 | 基于大纲知识点的覆盖分析（按章节分组） | ✅ | 「大纲覆盖分析」以章节行 + 知识点行两级表格呈现，含大纲要求（掌握/熟悉/了解）、学时、状态 |
| 5 | 综合覆盖率圆环 + 章节进度条 | ✅ | 顶部 conic-gradient 圆环显示综合覆盖率；每章节右侧有覆盖率进度条 |
| 6 | 部分覆盖 / 待开课知识点识别与提示 | ✅ | 状态列区分“已覆盖/部分覆盖/待开课（第X周）”，未开课知识点给出开课周提示 |
| 7 | 重新匹配 / 重新计算可操作并刷新 | ✅ | jsdom 实测：按钮转“匹配中/计算中…”后 toast 提示完成并刷新分析时间 |
| 8 | 输出 AI 课堂分析报告（知识点覆盖统计详情） | ✅ | 「分析报告」含报告编号、数据周期、覆盖统计详情表、薄弱知识点清单 |
| 9 | 报告下载（真实文件导出） | ✅ | 「下载报告」生成带知识点明细的 TXT 文件；「打印报告」走浏览器打印 |

### 自验结果
- 是否跑通：✅ 已亲手验证

---

## 演示参数 3：直录播资源 / 剪辑 / 权限 / 同步 + 语音转写与翻译

- 来源：演示参数
- 实现等级：真实实现
- 所属页面：资源中心 →「直录播资源」（`#/resources`）、「语音转写与翻译」（`#/stt`）、「云端资源库」（`#/cloud`）
- 实现方式：资源 CRUD 持久化；在线剪辑按“入点/出点”计算导出并生成“剪辑片段”资源；权限设置真实修改开放范围/开放能力；同步至教学平台带进度状态更新；语音转写按 2.9s/句真实滚动出字，语义纠错开关控制同音误识纠正标注，译文在 11 语种间实时重译。

### 实现明细（逐条）

| # | 交互点 / 需求 | 是否实现 | 如何验证 |
|---|---|---|---|
| 1 | 资源按课程/类型筛选与搜索 | ✅ | 课程下拉、类型 Tab（含各类计数）、关键字搜索均生效 |
| 2 | 视频资源播放（模拟课堂实录播放器） | ✅ | 点「播放」弹层播放器可播放/暂停/倍速/拖动进度，课件按时间切换、顶部显示 REC 标识 |
| 3 | 开放 / 访问权限管理（范围 + 能力） | ✅ | 「访问权限」弹窗选择校内公开/课程班级/指定班级/私有，保存后列表权限标签即时更新并写日志 |
| 4 | 在线剪辑并导出保存 | ✅ | 设置入点/出点后点「导出剪辑」→ 自动生成“剪辑片段”资源并出现在列表，含素材来源标注 |
| 5 | 同步至教学平台云盘 / 资源库 | ✅ | 点「同步教学平台」→ 按钮转“同步中…”→ 状态变为“已同步教学平台”，展示目标路径与时间 |
| 6 | 上传资源真实入列表 | ✅ | jsdom 实测：资源数 7→8，新资源出现在列表并提示“后台转码” |
| 7 | 云端资源库（文件夹/搜索/上传/权限/同步/下载/删除） | ✅ | 云端资源库目录 chips、文件表格齐全，上传/权限/同步/删除均为真实数据操作 |
| 8 | 语音实时转写为文字 | ✅ | 「开始实时转写」后文字逐句滚动出现，带时间戳、说话人、实时时长与字数统计 |
| 9 | 按前后文语义动态纠正文字 | ✅ | 开启“语义纠错”后，转写句出现绿色纠正标注：误识“满族废”→“慢阻肺”（结合上下文语义）；关闭开关则不做纠正，统计计数同步变化 |
| 10 | 实时翻译多语种（中英德法日俄西韩阿葡印） | ✅ | 11 个语种 chip 切换，译文实时重译并更新；jsdom 实测切到日语后首行译文为日语 |
| 11 | 转写记录保存 / 查看 / 导出 / 删除 | ✅ | 「保存转写记录」生成记录（jsdom 实测 1→2 条）；查看含正文与纠正标注，导出 TXT 含英语译文 |

### 自验结果
- 是否跑通：✅ 已亲手验证（含 11 语种切换、语义纠错）

---

## 演示参数 4：直录播直连督导评价

- 来源：演示参数
- 实现等级：真实实现
- 所属页面：督导评课 →「在线督导」（`#/supervision`）
- 实现方式：顶部紫色信息栏展示 课程/开课学院/授课教师/教室/班级/排课周/周进度/选课人数（随节次切换联动）；左屏播放课堂实录；右侧选择评价表 → 展示基础信息 → 逐项打分自动合计 → 提交归档；基础信息字号大/中/小一键切换。

### 实现明细（逐条）

| # | 交互点 / 需求 | 是否实现 | 如何验证 |
|---|---|---|---|
| 1 | 顶部栏展示课程、开课学院、教师、教室、班级、排课周、周进度、选课人数 | ✅ | 紫色渐变信息栏 8 格字段齐全，切换节次后字段联动更新 |
| 2 | 主界面直接查看授课视频 | ✅ | 左栏播放器播放对应课程课堂实录，视频源与“直连校方评价系统”标识可见 |
| 3 | 对接已有评价系统的评价表并选择 | ✅ | 「评价表」下拉仅列出已启用评价表（与「评价表管理」停用联动） |
| 4 | 评价界面展示评价表基础信息 | ✅ | 名称/版本/制定单位/适用范围/指标数/评分说明卡片展示 |
| 5 | 基础信息字号大中小切换 | ✅ | 字号切换「大/中/小」；jsdom 实测切换大字后评价区字号变为 16.5px |
| 6 | 按评价表指标打分，自动合计与等级 | ✅ | 打分实时合计；jsdom 实测全满分合计 100.0 分、等级“优秀” |
| 7 | 评价提交归档（真实入库） | ✅ | jsdom 实测：评价记录 2→3，提交后提示已提交校方评价系统，对应节次显示“已提交/可重新评分” |
| 8 | 历史评价查看 / 删除 / 导出 | ✅ | 记录列表完整，查看弹窗展示总分等级与评语，支持导出汇总 TXT |

### 自验结果
- 是否跑通：✅ 已亲手验证

---

## 平台整体（伪装成正式系统的其他导航）

- 来源：菜单参数（非演示参数，用于让平台观感是完整正式系统，而非按标书定制）
- 实现等级：可跳转页面 + 基础真实交互（无空白页）

| 导航项 | 状态 | 页面内容与可交互点 |
|---|---|---|
| 工作台 | ✅ | 欢迎条、4 统计卡、近 8 周活动趋势图、课程覆盖一览、待办、平台对接状态、动态流、快捷入口 |
| 我的课程 | ✅ | 课程卡片（进度条/活动/资源/覆盖数），一键进入课堂活动/授课计划/覆盖分析/直录播/督导 |
| 选课与排课 | ✅ | 排课明细表 + 冲突检测（真实扫描）+ 课表导出 |
| 云端资源库 | ✅ | 目录筛选/搜索/上传/权限/同步教学平台/下载/删除 |
| 评价表管理 | ✅ | 评价表卡片（维度/指标/满分/使用次数）、启停开关、查看结构、复制、删除、新增（含复制模板） |
| 用户管理 | ✅ | 角色筛选/搜索/新增/停启用/重置密码/删除 |
| 操作日志 | ✅ | 全模块操作留痕（自动追加），模块筛选、导出、清空（确认） |
| 帮助中心 | ✅ | FAQ 手风琴、平台服务状态、快捷入口、联系支持 |

### 自验结果
- 是否跑通：✅ 16 个路由全部渲染非空、无 404、无空白页

---

## 登录

| # | 交互点 | 是否实现 | 如何验证 |
|---|---|---|---|
| 1 | 登录页不显示账号密码任何提示 | ✅ | 页面无账号密码明文；仅“账号/密码/记住登录状态”正式表单 |
| 2 | 错误账号密码有提示 | ✅ | jsdom 实测：错误密码弹“账号或密码错误，请核对后重试”且不写入会话 |
| 3 | 正确账号密码进入系统 | ✅ | jsdom 实测：root/root 登录后写入会话并跳转 `app.html#/dashboard` |
| 4 | 登录态保持（刷新仍在系统内） | ✅ | 会话存 localStorage，重开 app.html 直接进入工作台 |
| 5 | 退出登录回登录页 | ✅ | 顶栏用户菜单「退出登录」二次确认后清除会话回 `index.html` |
| 6 | 修改密码真实生效 | ✅ | 修改后写入本地覆盖密码，下次登录须使用新密码 |

---

## 总览表

| 功能 | 实现等级 | 自验是否通过 |
|---|---|---|
| 登录（localStorage 会话） | 真实实现 | ✅ |
| 活动同步/时间轴/发起/跳转教学平台 | 真实实现 | ✅ |
| 授课计划比对与覆盖率 | 真实实现 | ✅ |
| 大纲知识点覆盖分析 | 真实实现 | ✅ |
| AI课堂分析报告（下载/打印） | 真实实现 | ✅ |
| 直录播资源管理（权限/剪辑/同步/上传） | 真实实现 | ✅ |
| 语音实时转写（语义纠错） | 真实实现 | ✅ |
| 11 语种实时翻译 | 真实实现 | ✅ |
| 督导评课（信息栏/打分/字号切换） | 真实实现 | ✅ |
| 评价表 / 用户 / 日志 / 课程等其余导航 | 可跳转页面 | ✅ |
